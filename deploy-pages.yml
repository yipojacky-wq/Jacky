name: Update realtime quotes and deploy Pages

on:
  push:
    branches: ["main"]
  schedule:
    - cron: "*/5 1-6 * * 1-5"
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Fetch TWSE realtime quotes
        run: node scripts/update-realtime-quotes.mjs

      - name: Verify quote output
        run: node -e "const q=require('./data/realtime-quotes.json'); if(!q.generatedAt || !q.quotes || q.quotes.length < 500) process.exit(1); console.log(q.generatedAt, q.quotes.length)"

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
